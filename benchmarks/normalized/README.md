# Normalized Benchmark Outputs

This directory stores normalized benchmark CSV outputs with a common schema.

Target schema:

engine,
model,
n_obs,
n_random,
workspace_ms,
implicit_derivatives_ms,
factorization_ms,
total_wall_ms,
peak_rss_kb,
success

